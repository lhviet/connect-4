.game-player.card([class.active]='isActive')
  .card-body.p-2
    .h6.text-center.pb-1 {{playerName}}
    .text-center
      img.rounded([src]='player.avatar', alt='Player Avatar')
    .card-text.text-center
      .pt-2 Coins: {{player.count}}
      .text-muted Win: {{player.win}}
